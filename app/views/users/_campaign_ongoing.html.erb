<p class="notice" style="color: orange; font-weight: 400;"><%= notice %></p>
<p class="alert" style="color: orange; font-weight: 400;"><%= alert %></p>
<p>BE THE FIRST TO KNOW <span>&</span> GET REWARDS</p>
<p class="no-newsletter">We will not send you marketing emails! Do not worry.</p>
<%= form_for @user, url: users_create_path, method: :post do |f| %>
  <%= f.text_field :email, :placeholder => "Enter Email" %>
  <input class="submit brandon" name="commit" type="submit" value="Step Inside" />
<% end %>
<%= link_to "Already subscribe on another divice?", already_subscribe_path %>
